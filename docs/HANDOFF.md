# 인계 메모

새 항목은 맨 위에 5줄 이내로 추가한다.

---

## 2026-08-16 · Claude Code

- **8/14의 "코드로 할 수 있는 1.0 작업은 전부 끝났다"는 틀렸다.** 거짓 약속 감사(감사관 2 + 수퍼바이저 반증)에서 21건 확정. 화면 문구가 약속하는 기능이 실제로는 없는 경우들이었다.
- 앱 잠금은 스위치만 있고 실행 시 인증 게이트가 없었고, AI 보정 스위치는 어떤 코드에도 연결돼 있지 않았다 — 둘 다 1.0에서 제거(local_auth·http 의존성, Face ID 문자열, 방침 문단까지).
- 만료 알림은 스위치를 켠 순간의 쿠폰만 예약됐다. `notificationSyncProvider`가 쿠폰 스트림을 구독해 재예약하도록 바꿨다. 쿠폰 삭제 시 이미지 파일도 남고 있었다(`ImageStore.remove()` 호출부 0건).
- `test/widget/capability_claim_test.dart`가 재발을 막는다 — 환경 의존 기능을 약속하는 화면은 capability provider를 읽어야 통과한다. 문구를 새로 쓸 때 이 테스트를 믿어라.
- 아직 실기기 검증은 아무도 못 했다. 감사는 전부 정적 분석이다.

## 2026-08-14 · Claude Code

- 이미지 검증 어댑터(T-017), 검토 판정 강화(T-018), 브랜드 사전 185개(T-013), 발행사 11곳(T-012), 파싱 테스트(T-014), 암호화 백업/복원(T-052)까지 완료. 테스트 131개 통과.
- 백업 형식: `CPDBAK01 | salt16 | nonce12 | AES-256-GCM | mac16`, 키는 PBKDF2-HMAC-SHA256 210k회. 복원은 병합(같은 id는 기기 우선, 이력 이중 차감 방지).
- 코드로 할 수 있는 1.0 작업은 전부 끝났다. 남은 것은 전부 사용자 몫: Xcode/Android Studio 설치(T-074), 계정(T-073), 실기기 검증, 심사 제출(T-076).
- 브라우저 패널 입력이 간헐적으로 막혀 백업 다이얼로그는 위젯 테스트로 검증했다 (test/widget/backup_dialog_test.dart).

## 2026-08-01 · Claude Code

- 1.0 기능 구현과 네이티브 설정을 마쳤다. `flutter analyze` 무경고, 테스트 52개 통과, 웹 릴리스 빌드 통과.
- 남은 것은 **도구 설치와 계정 등록에 막힌 항목뿐**이다 (`TASKS.md` T-073·074·076).
- Codex가 이어서 할 일: `T-012` 발행사 규칙, `T-013` 브랜드 사전 확충, `T-014` 파싱 테스트, `T-052` 암호화 백업.
- 파싱 규칙을 늘릴 때는 `test/unit/coupon_parser_test.dart`에 실제 문구 사례를 함께 넣는다.
- 실기기 검증(바코드 스캐너 인식, 알림 발송, OCR 정확도)은 아직 아무도 못 했다. Xcode/Android Studio 설치 후 최우선.
